import { DistrictCodeToNamePipe } from './district-code-to-name.pipe';

describe('DistrictCodeToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new DistrictCodeToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
